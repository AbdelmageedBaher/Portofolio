/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.mjs
  images: {
    domains: ['your-image-domain.com'], // Add the domains you're using here
  },

};


export default nextConfig;
